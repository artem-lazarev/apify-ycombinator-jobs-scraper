apify>=2.0.0
pydantic>=2.0.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# MCP connector support. 2.0 renamed the streamable-HTTP client and switched it
# to an injected HTTP client, so the <2.0 API will not work here. That client
# must be an httpx2.AsyncClient - httpx2 is a separate package from httpx, and
# mcp 2.x depends on it rather than on httpx.
mcp>=2.0.0
httpx2>=2.5.0,<3

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
