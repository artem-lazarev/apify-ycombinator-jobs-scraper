apify>=2.0.0
pydantic>=2.0.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# MCP connector support. 2.0 renamed the streamable-HTTP client and switched it
# to an injected httpx client, so the <2.0 API will not work here.
mcp>=2.0.0
httpx>=0.27.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
